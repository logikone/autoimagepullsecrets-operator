apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoimagepullsecrets-operator.fullname" . }}-test-connection"
  labels:
    {{- include "autoimagepullsecrets-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoimagepullsecrets-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
